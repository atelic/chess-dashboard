# Turso Database (https://turso.tech)
# Get these values by running:
#   turso db show chess-dashboard --url
#   turso db tokens create chess-dashboard

TURSO_DATABASE_URL=libsql://your-database.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# ==============================================
# Development Database
# ==============================================
# In development (NODE_ENV=development), a local SQLite file is used
# at ./data/dev.db instead of Turso. This keeps dev data separate from prod.
#
# To seed the dev database with production data:
#   npx tsx scripts/copy-prod-to-dev.ts
#
# To force using production database in development:
#   USE_PROD_DB=true npm run dev
